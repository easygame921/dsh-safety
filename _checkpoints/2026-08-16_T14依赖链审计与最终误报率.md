# T14 依赖链审计 + 键盘监听降噪 + 最终误报率 28.8%

> 生成时间：2026-08-16
> 上一阶段：误报率 64% → 34.8%（邻近语义/注释排除/T06 精确化等）

## 已完成

### T14 依赖链审计（新增威胁编号）

1. **T14-001 review — 依赖链投毒**（`pkg-deps-audit` FileCheck）：
   - 已知恶意包名单（事件实证）：event-stream、ua-parser-js 投毒版、rc、coa、
     is-promise、node-ipc、colors、faker.js、crypto-js、eslint-scope、babel-mixin、
     sequelize、user-agent-parser、ua-parser-js-pro
   - **typosquatting**：与高价值知名包（20 个基准）**长度相同 + OSA 距离 =1**
     （单字符替换或相邻交换：lodahs/lodash、axois/axios）
   - 可疑来源协议：file: / git+ / http://
2. **T14-002 review — registry 篡改**：`.npmrc` 指向非 https / 非官方源
3. **T14-003 notice — 通配版本**：* / latest（DSH 官方 @deepseek-ai/* 用通配是标准）
4. **files.ts dotfile 修复**：`extname('.npmrc')` 返回空串导致 dotfile 不扫描——
   按完整文件名匹配

### typosquat 算法演进（关键调试）

- v1 Levenshtein ≤2：误报 clsx/tsx、vitest/vite、@types/node vs zod（合法短词）
- v2 Levenshtein ==1：漏抓 lodahs/axois（相邻字符交换距离是 2 不是 1！）
- **v3 OSA（Optimal String Alignment）==1 且长度相同**：交换计 1 次编辑——
  lodahs/lodash ✓、axois/axios ✓、clsx/tsx ✗（不误报）、vitest/vite ✗ ✅

### T09 键盘监听降噪

- T09-002 改为 **keylog-exfil 组合**：键盘监听 + (网络外发/剪贴板/存储) 才 review
- 新增 T09-003 notice：单独键盘监听（命令面板/快捷键是中性能力）
- 本地真实插件误报 6 → 1 条（zat-dsh-engine 归零）

## 最终指标（68 真实社区插件）

| 指标 | 值 |
| --- | --- |
| **误报率（REVIEW 率）** | **28.8%**（19/66，目标 ≤40% ✅ 超额） |
| 恶意样本抓取率 | 100%（12/12） |
| 威胁覆盖率 | 100%（21/21） |
| 良性夹具误报 | 0 |
| 本地真实插件误报 | 1 条（prepare 脚本能力面项） |
| 测试 | 30/30 全绿 |

剩余 19 个 REVIEW 分布：T06 凭据助手 8、T03 安装脚本 6、T08 3、T04 2、
T09/T02/T05 各 1——均为"插件确实有该能力"的功能面合理项。

## 关键决策

- 已知恶意包名单只收录"事件实证"，typosquat 由通用算法覆盖（不硬编码拼写变体）
- typosquat 判定用 OSA 而非 Levenshtein（交换是 typosquat 主流手法）
- 键盘监听"能力面"降 notice，但保留组合升级路径（监听+外发仍 review）
- dotfile 按完整文件名匹配（extname 语义缺陷）

## 下一步

- 剩余能力面 REVIEW 处理：T06 凭据助手/T03 安装脚本 → 动态沙箱复核为主（静态保留）
- 动态沙箱 ctx mock 扩展
- 1.0 发布准备（规则库版本号、文档一致性）
