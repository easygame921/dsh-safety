# 2026-08-16 M3 增强版 + MCP 装配 + 自动触发

## 已完成
- M3 增强版：P1 规则（T07 prompt注入 / T08 思维链劫持 / T09 client钓鱼 / T10 持久化驻留 / T11 DNS外带）+9 条
- 白名单机制：AllowlistConfig（插件名/路径/主机 + 规则id），skip/downgrade，默认白名单（harness-pet/dsh-vision/better-sidebar），报告「白名单抑制」段
- 新夹具 5 个恶意样本；测试 17/17（P0 6 + P1 5 + 白名单 3 + 良性 2 + 契约 1）
- MCP 装配到 web profile（cordis.patch.yml 加 dsh-mcp-client 条目，serverName: safety）
- 验证：list_rules 21 条 ✓、audit_plugin harness-pet ✓（REVIEW→OK，白名单降级生效）
- 自动触发规范：写入 ~/.dsh/AGENTS.md（先审计后安装）+ docs/AUTOTRIGGER.md

## 关键决策
- 白名单默认 downgrade 而非 skip（保留证据可追溯）
- 自动触发走 agent 指令层（MCP 是被动工具，无法监听 DSH 事件；AGENTS.md 引导 agent 自觉）
- 文件报告由 agent 用文件工具落盘（无需改 MCP）

## 当前状态
- git：ef5b831（M3 完成），工作树有 _checkpoints/_decisions 待提交
- 装配路径：D:\学习\个人demo\dsh-plug\dsh-safety\dist\cli.js（绝对路径）

## 下一步
- 用户验证自动触发（说"想装 xxx 插件"）与文件报告输出
- M4 开源发布（用户决定后再做）
